# 非敏感部署参数（入库）。版本号唯一来源：根目录 VERSION 文件，
# 由 `bash deploy/sso release` 生成为 <VERSION>-<git sha> 并自动写回本文件。
CASDOOR_IMAGE_NAME=casdoor-sso
CASDOOR_VERSION=0.0.0-unreleased
CASDOOR_PORT=8000
MYSQL_IMAGE=mysql:8.0.25
MYSQL_DATABASE=casdoor
